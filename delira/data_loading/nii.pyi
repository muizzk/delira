# Stubs for delira.data_loading.nii (Python 3)
#
# NOTE: This dynamically typed stub was automatically generated by stubgen.

import abc
import numpy as np
from abc import abstractmethod
from typing import Any, Dict, Type

logger: Any

def load_nii(path: str) -> np.ndarray: ...
def load_sample_nii(files: dict, label_load_cls: Type) -> Dict[str, np.ndarray]: ...

class BaseLabelGenerator(metaclass=abc.ABCMeta):
    fpath: Any = ...
    data: Any = ...
    def __init__(self, fpath: str) -> Any: ...
    def _load(self) -> Any: ...
    @abstractmethod
    def get_labels(self) -> Any: ...
